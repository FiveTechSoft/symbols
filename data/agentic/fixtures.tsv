# fixtures.tsv — external tool fixtures (stand-ins, never KB).
# Typed contract: rel rows take 4 columns, person rows 3 columns.
#   rel SUBJECT REL OBJECT
#   person NAME DETAIL (spaces allowed, tabs split only)
# Loaded once with tools.tsv; unknown TYPEs and malformed rows warn
# + discard (fail-closed). Missing file or no valid rows -> frozen
# compiled fallback. Read-only; fixed path; file order is priority.
rel	babylonia	rey	nebuchadnezzar
rel	david	mother	nitzevet
rel	saul	rey	israel
person	jonas	nacio en Gathepher
person	jesse	nacio en Bethlehem
