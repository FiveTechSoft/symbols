# tools.tsv — external agentic data tables (like bible_relations.tsv).
# TYPE KEY A B   (allow rows accept an optional 5th column = default argv)
# Loaded once by ToolInit(); unknown TYPEs and malformed rows are
# warned + discarded (fail-closed, never abort, never invent).
# Missing file or empty table -> frozen compiled fallback.
# This file is read-only from the engine: no code regenerates it
# and its path is fixed (no env override, no CLI flag).
TYPE	KEY	A	B
allow	shell	echo	cmd
allow	shell	dir	cmd
allow	shell	ls	bash
allow	shell	pwd	bash
allow	shell	powershell	powershell	Get-Location
allow	shell	bash	bash	pwd
allow	shell	gcc	direct	--version
contract	reigns	lookup_relation	open
contract	taxonomy	lookup_relation	known-only
contract	father	lookup_relation	known-only
contract	sibling	lookup_relation	known-only
contract	wife	lookup_relation	known-only
contract	parent	lookup_relation	known-only
contract	children	lookup_relation	known-only
contract	grandparent	lookup_relation	known-only
contract	descendant	lookup_relation	known-only
