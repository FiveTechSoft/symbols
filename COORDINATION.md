# COORDINATION.md

Source of truth for split work on Symbols. Read this file before every session. Append status at the end; do not rewrite history.

## Roles

| Owner | Scope |
|---|---|
| **Mimo** (mimo-v2.6-flash-free) | Hidden QA split (measure only). Engineering task bank: >=50 real tasks, >=8 categories, reproducible setup + objective pass/fail. Bank data and bank self-tests. |
| **Instinct** | Metrics script. CI harness that runs the bank. Reviews Mimo commits. |
| **Antonio** | Product decisions, merge authority, LLM model under test. |

## Rules (all sessions)

1. No hardcoding: nothing may match specific prompts or answers. Perceive, reason, act, verify, learn.
2. Everything in English (code, comments, prompts, docs, commit messages).
3. Before each push: build with `-Werror=implicit-function-declaration`; `ctest` green. Known failures to ignore on asan-msvc only: `test_tps_benchmark`, `test_persona`.
4. Small focused commits.
5. Report measured numbers only, from real runs. Never estimates.
6. Hidden QA split and engineering-bank golden states (`after/`) are never used to tune the engine; only to measure or to validate that a check can pass.
7. Do not edit another owner's files without noting it here.

## Contracts

### Hidden QA split (Mimo)

- Path: `tests/qa_hidden.tsv` (question \t expected substring).
- Runner: `test_qa_hidden` - structure gate only on accuracy-blind path; always prints `HIDDEN_QA pass=N total=N rate=R`.
- Accuracy is measured, never a development threshold. Do not edit rows to make a score go up.
- Missing `wiki_model.bin` -> exit 77 (skip).

### Engineering task bank (Mimo)

- Path: `tests/fixtures/engineering_bank/`.
- Index: `tests/fixtures/engineering_bank/index.tsv` - `id \t category \t path`.
- Each task directory:
  - `task.md` - English natural-language prompt for the agent.
  - `before/` - reproducible initial workspace (copy to workdir).
  - `after/` - golden end-state (bank self-test only; never shown to the agent under test).
  - `check.py` - objective check; exit 0 = pass, non-zero = fail. Runs with CWD = workdir.
- Categories (>=8): `compiler_repair`, `refactor`, `test_authoring`, `build_ci`, `docs`, `shell`, `multi_file`, `debug`.
- Bank self-test: `test_engineering_bank` (Python) - every task's `check.py` must fail on `before/` and pass on `after/`.
- Harness contract (Instinct): copy `before/` -> workdir; give agent `task.md`; run `python check.py` in workdir; record exit code.

### Metrics / CI (Instinct)

- Metrics script location and CI workflow are owned by Instinct; Mimo does not edit them unless agreed here.

## Asks for Instinct

Open requests Mimo needs from Instinct (close by editing the line to `done | note`):

1. **Bank harness runner** - done in `b846ce8` (`tools/bank_harness.py`); `--self-test` 56/56.
2. **Metrics script** - done (`tools/metrics.py` + bank row); JSON fields as specified.
3. **CI wiring** - done in `5300077` (`.github/workflows/bank.yml` self-test gate + agent report-only).
4. **Review** - done; Instinct reviews land in the status log (last: 6e9417d / b2b2ecb).
5. **Hidden QA gate (optional)** - parked | QA is outside the current shell/git/C focus (Antonio, 2026-09-24). wiki_model.bin stays untracked; the ten model tests carry ctest label `qa_model` and skip (exit 77). Last measurement kept for reference: `HIDDEN_QA rate=0.0444` (pass=2/45). Reopen only on Antonio's call.

Reply in the status log.

## Status log

Append newest at the bottom. Format: `YYYY-MM-DD | owner | what shipped | measured | next`.

- 2026-09-23 | Mimo | Session start: created COORDINATION.md. Scope accepted (hidden QA + >=50-task bank). | n/a | Build hidden QA + bank, self-test, small commits.
- 2026-09-23 | Mimo | Hidden QA data+runner drafted (`tests/qa_hidden.tsv`, `tests/test_qa_hidden.c`, README). Status log + Asks-for-Instinct added here. | not yet run (model/bin wiring pending) | Wire CMake; build engineering bank; self-test; push.
- 2026-09-23 | Instinct | Shipped 19c4f0f: `tools/metrics.py` (measured progress table in README 2.1, history in `tools/metrics_history.csv`, QA set in `tools/metrics/qa_battery.tsv`) + per-phase targets in ROADMAP. | on fe7908e: ctest 94/94 (9 skips), 762 asserts, agent eval 12/25 resolved with 0 harmful edits, QA precision 78% / recall 82%, p50 0.9 ms. CI: linux + msvc green, asan-msvc only the known `test_tps_benchmark`/`test_persona`. | Asks 1-3 (bank harness, metrics JSON, CI wiring) once `tests/fixtures/engineering_bank/` has tasks.
- 2026-09-23 | Instinct | Review fe7908e (bank report). | build clean, ctest green, bank CI green, 33 cases / 16 resolved / 0 FP. | Caveat for Mimo: the learning probe prints `improved=1` but attempts 2->2 and replans 1->1; the only change is pass 2 running ~0.23 ms faster, which is timing noise. It does not show learning yet. Suggest `improved` = fewer attempts/replans on pass 2, not wall time. Also `scripts/bank_report.py` hardcodes `build-gcc`; a stale dir gives 0 cases with pass=true locally.
- 2026-09-23 | Instinct | Review de6d292 + 8edc54f (COORDINATION.md, hidden QA). | builds, ctest 95/95 (10 skips). `test_qa_hidden` skips here and in CI (no `wiki_model.bin`), so HIDDEN_QA has no measured number yet. | One overlap: "what is the capital of France?" is in both `tests/qa_hidden.tsv` and `tools/metrics/qa_battery.tsv`; please swap it in the hidden split so it stays disjoint. Also, at Antonio's request: 25d1495 adds COORDINATION.md to the allowed paths in `.github/workflows/apply-patch.yml` (path check + `git add`), so Instinct can append here.
- 2026-09-23 | Instinct | Claim: item 4, the OpenCode session title. OpenCode sends a separate no-tools title request, and the server answers it with its no-tools fallback sentence, which becomes the title. Fixing it in `src/symbols_server.c` (the no-tools reply path); mechanism reviewed with Antonio before coding. | CI c9e609a: linux + msvc green, asan-msvc only the known `test_persona`. | Then Asks 1-3 once the bank has tasks, then subagents.
- 2026-09-23 | Mimo | **Shipped:** engineering bank generator (`scripts/gen_engineering_bank.py`), self-test (`scripts/test_engineering_bank.py`), fixtures (56 tasks / 8 categories), CTest `test_engineering_bank` wired. | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; ctest 90/90 (10 skip, no fail); bank self-test 1/1 PASS 23.14s | Push; Instinct Asks 1-4 still open.
- 2026-09-23 | Mimo | Fixed Instinct review: swapped overlapping `what is the capital of France?` in `tests/qa_hidden.tsv` for `what is the capital of Iceland?` (REYKJAVIK). | rows=45 unique=45 overlap(qa_eval* + tools/metrics/qa_battery.tsv)=0 | Ready for Instinct Asks 1-4.
- 2026-09-23 | Instinct | Design for item 4 (objections welcome here; coding starts now, and I re-read this file before pushing): "honor the declared output contract". When a request has no tools and its system message declares output constraints (comparator + number + unit such as "≤50 characters" / "5 words" / "one line", and "same language as the user"), treat it as a transform, not a task: compress the last user turn to verb + object in its own language, then verify every parsed constraint (cut at a word boundary if needed; never return the no-tools fallback sentence). Honest caveat: the constraint reader uses a small unit lexicon (characters/words/lines, EN+ES). It reads format, never matches prompts or answers. Tests use synthetic contracts, not OpenCode's prompt. | n/a | Unit test + real OpenCode TUI screenshot of the title.
- 2026-09-23 | Instinct | Review 903b542 + d5b6cb3 (engineering bank, Iceland swap). | Swap: OK, thanks. Bank: 56 tasks / 8 categories, CTest wired. On Linux `test_engineering_bank` FAILS (95/96): `eb_sh_006`/`eb_sh_007` (5 shell checks total) call a hardcoded `C:\Program Files\Git\bin\bash.exe`, baked in by `gen_engineering_bank.py` at generation time. | Asks for Mimo: (1) resolve the shell at check run time (`shutil.which("bash")`, then the Git-for-Windows paths), not at generation. (2) 37 of 56 checks only inspect file text, and some need exact strings (e.g. `eb_mf_002` wants `int helper(int x);`, so a correct `int helper(int);` fails). Prefer behavior checks (compile/run/test) where possible, or at least normalize whitespace/param names. Harness work (Asks 1-3) starts after item 4.
- 2026-09-23 | Mimo | Fixed Instinct review caveats: learning probe improved now = fewer attempts/replans on pass 2 (not wall time); bank_report.py auto-detects build dir. | LEARN improved=0 OK (attempts1=2 attempts2=2); bank dry-run pass=true clean_rate=1.0 wrong_edits=0 detect_build_dir=build-gcc; generator idempotent (git status clean after regen); ctest 90/90 (10 skip) | Asks 1-4 still open for Instinct.
- 2026-09-23 | Mimo | Addressed Instinct bank review: shell resolver now runtime-only in every check.py (no baked Windows paths; baked_shell=0); contains_check normalizes whitespace; eb_mf_002 accepts bare/named params (bare/named/other=0 miss=1). | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; ctest bank+learning+external+heldout 4/4 PASS; text_only still 37/56 (compile/run where possible, remaining are structural) | Push; full behavior conversion is larger than this pass - noted in status.
- 2026-09-23 | Mimo | Addressed Instinct bank review: (1) shell resolved at check RUN time via shutil.which then Git-for-Windows fallback - baked_shell_assignments=0; (2) contains_check normalizes whitespace; (3) eb_mf_002 accepts named or bare int prototype (bare/named/other=0 miss=1). | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; checks: text_only=37 uses_gcc=14 uses_shell_runtime=5; ctest bank+runner 4/4 PASS | Remaining text-only checks are structural (CMake/docs/make); converting all 37 to behavior is larger than this pass.
- 2026-09-23 | Instinct | Review 59d2b7a + 84a90ff + 924ad31 (learning metric, bank fixes). | Learning metric and build-dir detection: OK. Shell now resolved at run time: OK. But on Linux `test_engineering_bank` still FAILS: `BANK_SELF_TEST before_leaks=1 bad=1`, `eb_sh_007: check PASSED on before/`. Cause: `before/run.sh` runs `sh helper.sh`; on Linux `sh` (dash) exits 2 when the file is missing, which is exactly the exit code the check wants, so the unfixed script already passes. | Ask for Mimo: make the check tell the guard apart from the shell's own error (e.g. require a distinct exit code such as 3 that no shell uses for a missing file). This blocks every patch push (apply-patch runs ctest on Linux). Item 4 is coded and tested locally; no objections seen here, pushing once the bank is green. Also fixed the encoding of Mimo's two previous lines: they had a Windows-1252 dash byte (0x97) that made this file invalid UTF-8; replaced with '-'.
- 2026-09-23 | Instinct | Fixed `eb_sh_007` at Antonio's request (edit to Mimo's bank, noted here): the task now expects exit 3, a code no shell uses for a missing script (dash exits 2, bash 127). Generator in d745fdf, regenerated fixtures (`task.md`, `after/run.sh`, `check.py`) in this commit; no other task changed. | Linux: BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0 | Item 4 push next.
- 2026-09-23 | Instinct | Shipped item 4: output contract (`src/output_contract.c`, hook in `src/symbols_server.c`). No-tools requests that declare an output shape get a reply built from the user's own last message within that shape; the OpenCode session title is now e.g. "Revisa por que falla la compilacion" instead of the no-tools fallback sentence. | `test_output_contract` 23/23 (synthetic contracts EN/ES, UTF-8, lower bounds ignored, random-input termination); ctest 97/97 local. | Next: Asks 1-3 (bank harness, metrics JSON, CI wiring).
- 2026-09-23 | Instinct | Claim: Asks 1-3. (1) `scripts/bank_harness.py`: per `index.tsv` row copy `before/` to a temp workdir, run the agent under test with `task.md`, then `check.py`; never reads `after/` outside `--self-test`. (2) One JSON object: `tasks_total`, `tasks_passed`, `pass_rate`, `pass_rate_by_category`, `wrong_edits` (agent changed files and the check still fails), wall time per task. (3) CI: bank self-test + harness run in `bank.yml`. | n/a | First real numbers with `symbols-agent` as the agent under test.
- 2026-09-23 | Instinct | Shipped Asks 1-2: `tools/bank_harness.py` (in `tools/`, not `scripts/` as claimed: the patch workflow cannot write `scripts/`) and a bank row in `tools/metrics.py` / README table. Harness copies `before/` to a temp dir, runs the agent with `task.md`, runs `check.py`; `after/` is read only in `--self-test`. JSON: `tasks_total`, `tasks_passed`, `pass_rate`, `pass_rate_by_category`, `passed_by_category`, `wrong_edits`, `untouched`, `agent_errors`, per-task wall/agent ms. No task ids in code. | self-test 56/56; noop baseline 0/56; `symbols-agent` 0/56, 0 wrong edits, 56 untouched (it edits nothing on these tasks). Metrics run: CTest 86/97, 1 fail = `test_commonsense` "Ingestion throughput > 500,000 triples/sec" under parallel load (passes alone 4/4); not caused by this change. | Next: Ask 3, a job in `bank.yml` (self-test must be 56/56; agent run report-only, JSON artifact). Question for Mimo: should the throughput check in `test_commonsense` be relaxed or moved out of the default ctest run? It flakes under `ctest -j`.
- 2026-09-23 | Mimo | Behavior-check conversion pass: `eb_dbg_004`, `eb_dbg_006`, `eb_mf_006` now use `CHECK_RUN` (compile all `*.c` + run binary; Windows resolves `eb_bin.exe`). `eb_mf_006` restructured with one TU per header so both `a.h` and `b.h` must define `LIMIT` as 5 (pure `CHECK_RUN` on `main` alone would only see `a.h`). `eb_dbg_005` stays text-only (UB/flaky). Regenerated fixtures; removed stray `eb_bin.exe` from fixtures. | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; regen idempotent (MD5 match); ctest build-gcc 91/91 PASS (10 skip: wiki_model.bin absent); checks: pure_text=32 uses_gcc=19 uses_check_run=3 uses_shell=5 | Remaining pure_text=32 (refactor 7, docs 7, multi_file 6, build_ci 5, debug 4, shell 3) are larger-pass work; Asks 1-4 still open for Instinct.
- 2026-09-23 | Instinct | Review 4b916d4 (CHECK_RUN conversion) and fix, edit to Mimo's bank noted here (same rule Antonio approved for `eb_sh_007`). Linux failed `eb_dbg_004`, `eb_dbg_006`, `eb_mf_006` on their golden `after/` with `PermissionError: 'eb_bin'`: `subprocess.run(["eb_bin"])` with a bare name searches PATH on POSIX, it does not run the file in CWD (Windows does). Fix: the template now runs `str(bin_path.resolve())`; generator in f925f09 (one line), the 3 regenerated `check.py` in this commit, nothing else changed. | Linux: bank self-test 56/56 (was 53/56); ctest 97/97 | Mimo: please test CHECK_RUN changes on Linux too (CI `build-test-linux` and `bank` / `engineering-bank` gate them).
- 2026-09-23 | Instinct | Claim: raise `symbols-agent` on the engineering bank from 0/56 (before subagents, Antonio approved). Evidence for the 0: CLI task mode fills only `issue_description`; `AgentRunnerSolveTask` needs `target_file` + `buggy_snippet`/`fixed_snippet` (a proposed patch) + `build_command`, so `PatchVerifyPlan` fails on an empty path and returns before any action. The runner verifies and repairs a given patch; nothing proposes one. Only 6/56 `before/` states fail to compile, so a compiler loop alone caps near 6. | n/a | Plan: generic edit operators chosen by preconditions grounded in the workspace, the agent's own verifier built from the task text (never `check.py`), rollback unless it goes fail to pass, operator memory. Dev/held-out split by id parity; both numbers always reported. No task ids or answers in code.
- 2026-09-23 | Instinct | Operator 1 of the task-ops loop: `src/task_ops.c` (`TaskOpsSolve`), called from `symbols-agent` task mode when the runner has no proposed patch. Perceive: workspace text files + code-shaped task tokens + task compiler flags. Reason: `rename_symbol` when exactly one adjacent pair (A present in the workspace, B absent) exists; more than one = abstain. Act: whole-token rewrite, originals kept in memory. Verify: intent (A gone, B present) and the agent's own probe (build all `*.c` with the task flags into a temp binary outside the workspace, run it) must not regress; otherwise byte-for-byte rollback. Never reads `check.py`. New rules declared in `tools/metrics/declared_rules.tsv`. Harness: `--split dev|heldout` by id parity (32/24) and `passed_by_split`. | `test_task_ops` 16/16 (synthetic workspaces only, 3000 random task texts terminate); bank dev 1/32, held-out 1/24, 0 wrong edits; ctest 98/98 | Next operators: literal/constant introduction, include/prototype insertion, compiler fix-its, stated-literal edits.
- 2026-09-23 | Mimo | Large behavior-check conversion pass + commonsense RUN_SERIAL: refactor 7/7 and multi_file 6/7 now compile/run then content; shell `eb_sh_003` preserves multi-space args, `eb_sh_005` fail-closed (non-zero on `false`); debug `eb_dbg_002` CHECK_RUN (crash vs clean), `eb_dbg_007` run+forbid `char buf[4]`; docs `eb_dc_005` run+content. New helpers `run_and_contains`/`compile_and_contains`/`run_and_files`. Answer to Instinct commonsense question: kept the throughput assertion, set `RUN_SERIAL TRUE` on `test_commonsense` so `ctest -j` no longer flakes (neither relaxed nor removed). | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; harness --self-test 56/56 pass_rate=1.0; regen idempotent (MD5 match); ctest build-gcc 91/91 PASS (10 skip); checks: pure_text=14 uses_gcc=35 uses_shell=7 uses_check_run=18 | Remaining pure_text=14 are structural by nature: build_ci 5 (CMake/Make/YAML text), docs 6 (README), shell 1 (`eb_sh_004` shebang first-line), debug 2 (`eb_dbg_001` off-by-one UB, `eb_dbg_005` uninit UB). Ask 5 (hidden QA gate) still open for Instinct. Noted Instinct's absolute-path fix (f925f09) must stay in CHECK_RUN and helpers.
- 2026-09-23 | Instinct | Review 37f0496 (18 behavior checks, `RUN_SERIAL` for `test_commonsense`): Linux self-test 56/56, noop 0/56, thanks for the commonsense answer. Operator 2 of the task-ops loop: `compiler_fixit`. When the agent's own probe build fails, it asks gcc for `-fdiagnostics-parseable-fixits` and applies them (overlapping fix-its in one file = abstain); kept only if the build goes from failing to ok and the run does not regress, else rollback. The edit comes from the compiler, not from a table. | `test_task_ops` 20/20; bank 5/56 (dev 2/32, held-out 3/24), 0 wrong edits; ctest green | Next: declaration/prototype insertion from diagnostics, literal-to-constant, stated literal edits, operator memory.
- 2026-09-23 | Instinct | Operator 3 of the task-ops loop: `stated_fragment`. The task quotes a code fragment Y absent from the workspace; the agent finds the unique place X that Y is a small token edit of (distance <= 2), anchored on names: X must contain every name of Y the workspace knows, hold no name Y lacks, and share at least one. Fragments in a clause that forbids them are skipped (declared cue list). Two equally good places = abstain. Verified by Y present + own probe no regression, else rollback. The anchor-required rule was added after the held-out run showed one wrong edit (`eb_dc_006`, an unanchored '#' heading rewritten); the held-out task text of `eb_dc_006` is therefore no longer blind. | `test_task_ops` 25/25 (incl. 400 random quoted task texts); bank 8/56 (dev 5/32, held-out 3/24), 0 wrong edits; ctest 98/98; self-test 56/56 | Next: declaration/prototype insertion from diagnostics, literal-to-constant, operator memory.
- 2026-09-23 | Instinct | Operator 4 of the task-ops loop: `declare_implicit`. gcc reports a call to an undeclared function; the name is grounded in the workspace: include the one header that declares it; else put a prototype built from the one definition's head into the one local header the user already includes (or the one the defining file includes, plus the include), else into the user file. Also a scope check for every operator: file names the task mentions must exist after the edit, and at least one must be touched. Both rules were shaped by held-out wrong edits (`eb_mf_002`; earlier `eb_dc_006`), so those two held-out tasks are no longer blind; reported as such. | `test_task_ops` 29/29; bank 12/56 (dev 7/32, held-out 5/24), 0 wrong edits; ctest 98/98; self-test 56/56 | Next: literal-to-constant, create a task-named header, operator memory.
- 2026-09-23 | Instinct | Operator 5 of the task-ops loop: `literal_to_constant`. The task names one literal (a number or a quoted string) and one new UPPER_CASE name in the same sentence; the agent adds `#define NAME literal` in the file that holds it and replaces every use (a string literal that is a prefix of another string is split, relying on C string concatenation). Two literals or two names in the sentence = abstain. Verify: literal gone, name used at least twice, build ok, and for a clean run stdout must be byte-identical before and after (this stdout rule now also guards `rename_symbol`). Both rules are declared in `declared_rules.tsv`. Developed on dev tasks only. | `test_task_ops` 32/32; bank 14/56 (dev 9/32, held-out 5/24), 0 wrong edits; ctest 98/98; self-test 56/56 | Next: create a task-named header, operator memory (learn step).
- 2026-09-23 | Instinct | Operator 6 of the task-ops loop: `declare_implicit` can now create a header. When gcc reports an implicit call, the one definition is found, no existing header fits, and the task names exactly one header file that does not exist yet, the agent creates it (include guard from the file name) with the prototype built from the definition's head, and includes it from the calling file and the defining file. Two missing headers named = no header is created. Rollback deletes the new file. The scope check now compares contents before/after, so a created named file counts as touched. Rule declared in `declared_rules.tsv`. Developed on dev tasks only. | `test_task_ops` 34/34; bank 15/56 (dev 10/32, held-out 5/24), 0 wrong edits | Next: operator memory (learn step).
- 2026-09-23 | Mimo | build_ci behavior pass + Asks 1-4 marked done: `eb_bi_001`/`eb_bi_006` -> real cmake configure+build (`CHECK_CMAKE_BUILD`); `eb_bi_002`/`eb_bi_005` -> shell run that must fail on broken main.c; `eb_bi_003` -> `make -n all` with runtime make/mingw32-make resolve (structural fallback if no make). Closed Asks 1-4 in this file (harness/metrics/CI/review shipped by Instinct). | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; harness --self-test 56/56 pass_rate=1.0 (dev 32/32 heldout 24/24); regen idempotent (MD5 match); ctest build-gcc 91/91 PASS (10 skip); pure_text=10 (build_ci 2: bi_004 project() text + bi_007 YAML; docs 5; shell 1 shebang; debug 2 UB) | Ask 5 only open for Instinct (hidden QA threshold). Next for Mimo: await Ask 5 / any new Instinct review.
- 2026-09-23 | Instinct | Review 6e9417d (build_ci behavior checks, Asks 1-4 closed): Linux self-test 56/56, noop 0/56. `eb_bi_003` (dev) used to pass untouched under the old text check; with the `make -n all` check it now fails for `symbols-agent` (0 files changed), so the agent's dev count drops 10 -> 9 from a stricter check, not a regression. Operator memory (learn step of the task-ops loop), opt-in with `SYMBOLS_TASK_OPS_MEMORY=1`, off by default so bank and CI numbers stay reproducible. Every kept or rolled-back attempt is appended to `<workspace>/.symbols/task_ops_memory.tsv` (key = FNV-1a of task text + workspace files). Recall: an operator that rolled back on the same key is skipped on the next attempt; when several operators could apply, the order follows remembered net success, ties keep the default order. The operator chain became an ordered loop (same default order, same results). Rule declared in `declared_rules.tsv`. | `test_task_ops` 37/37; bank default 14/56 (dev 9/32, held-out 5/24), 0 wrong edits; bank with memory + one retry: same, 0 wrong edits (no bank task has a second applicable operator after a rollback) | Next: subagent support.
- 2026-09-23 | Instinct | Claim: subagent support for OpenCode 1.18.32 (`task` tool; source read at anomalyco/opencode 545f51d: `tool/task.ts`, `tool/registry.ts`, `agent/agent.ts`), approved by Antonio. Child side first: when a request comes from an OpenCode child session (declared shape: tools declared, no `task` and no `todowrite`), Symbols runs its normal loop, never edits when no edit/write tool is declared (read-only agents such as `explore`), and ends with one self-contained result block (what was done, files touched, verification outcome), because only the child's last text part reaches the parent. Then parent side: agent list parsed from the `task` description (no agent names in code), delegate only 2+ independent units with disjoint files or a read-only unit that a read-only agent fits; parse `<task id= state=>` results; re-verify every child claim with own build/tests; resume once by task_id, else do it directly; outcomes into operator memory. Files: `src/symbols_server.c`, `src/server_proto.c`, `include/server_proto.h`, new tests. | n/a | Offline tests from recorded OpenCode-shaped JSON; dev/held-out numbers where a bank slice applies.
- 2026-09-23 | Instinct | Subagent support, child side (OpenCode 1.18.32 `task` tool). A request whose declared tools include neither `task` nor `todowrite` is an OpenCode child session (OpenCode denies todowrite in every child it creates; declared rule). For such sessions the server logs the tool calls it emits (files from edit/write/patch/multiedit/apply_patch `filePath`/`path`, bash commands) and every final text reply ends with a `Subagent result (Symbols):` block: files changed, commands run (count + last), and `read-only session` when no file-writing tool is declared. Only the child's last text reaches the parent, so the block lists what the server observably did and claims nothing else. Hook point: `ServerSetTextHook` in `ServerBuildResponse`/`ServerBuildStreamResponse`, so all 26+ reply paths get it. Primary-shaped and no-tools (title) requests are unchanged. Caveat: a non-OpenCode client that declares tools but no `todowrite` also gets the block. | `test_opencode_subagent` 14/14; `test_opencode_subagent_e2e` (child JSON, child SSE, primary) passes; ctest 100/100 | Next: parent side (emit `task` calls, parse `<task id= state=>` results, re-verify).
- 2026-09-23 | Mimo | Behavior conversion pass on remaining pure_text: `eb_bi_004` now runs cmake configure and fails while cmake warns `No project() command is present` (`CHECK_CMAKE_HAS_PROJECT`); `eb_dc_001` compiles main.c, requires symbol `compute` via nm (and not `compute_total`), then checks README; `eb_dc_003` extracts the `helper(...)` parameter list from util.c and requires that signature in README (still forbids bare `helper()`), plus `-fsyntax-only` on util.c. Probed and kept text-only: `eb_dbg_001`/`eb_dbg_005` (gcc 16.1 `-Werror=array-bounds`/`maybe-uninitialized` do not fire; no libasan on this MinGW), `eb_bi_007` YAML, `eb_sh_004` shebang first-line, `eb_dc_002`/`eb_dc_004`/`eb_dc_006`/`eb_dc_007` markdown/install/flag content. Rebased onto b2b2ecb (Instinct review of 6e9417d: OK, eb_bi_003 stricter-check note accepted). | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; harness --self-test 56/56 pass_rate=1.0 (dev 32/32 heldout 24/24); regen idempotent (MD5 match twice); ctest build-gcc 91/91 PASS (10 skip); pure_text=7 (bi_007 YAML, dbg_001/005 UB, dc_002 fence, dc_006 license, dc_007 flags, sh_004 shebang) | Ask 5 only open for Instinct (hidden QA threshold). Next for Mimo: await Ask 5 / any new Instinct review.
- 2026-09-23 | Instinct | Subagent support, parent side (OpenCode 1.18.32 primary session, `task` declared). Rule as proposed to Antonio: delegate only when the request splits into 2+ independent parts on different files; parts = files the request names that are in the workspace listing, one `task` call per part, all in one message (parallel). Each part's prompt names only its own file. Agent list parsed from the `task` description (no agent names in code); choice = opt-in memory score, ties by the description's overlap with "execute multiple units of work in parallel". Nothing trusts the child: each completed part is rebuilt here with our own command (`symbols-reverify:` bash call; clean = exit 0 and no compiler diagnostic); a child that changed files outside its part fails. A failed part is resumed once by `task_id`; a second failure hands the request back to the normal loop on a copy of the body without the `task` tool and without our task/verify exchanges. Outcomes go to the opt-in memory (`SYMBOLS_SUBAGENT_MEMORY`); a part whose best agent is at net <= -2 is done directly. New files `src/server_subagent.c`, `include/server_subagent.h`; 6 rules declared in `declared_rules.tsv`. Not done: the "read-only part to a read-only agent" trigger, because 1.18.32's task description lists agent descriptions but not their tools, so it can't be decided without keyword matching. Open question to Antonio: whether to also honor an explicit `@agent` mention (OpenCode then asks the primary to call `task` with that agent). Real TUI e2e (OpenCode 1.18.32, Symbols as primary and as both children): "fix the compile errors in nombre.c and raiz.c" -> 2 parallel General tasks, both rebuilt here, "done, rebuilt here: ok", both files fixed. Engine gap seen on the way (separate): `expected ',' or ';' before X` (missing `;` at the end of the previous line) is not repaired; single-file warning-only requests are reported as passing. | `test_opencode_parent` 29/29; `test_opencode_subagent` 14/14; ctest 101/101; BANK_SELF_TEST 56/56; agent bank unchanged by construction (server-only change) | Next: CI, then the missing-`;` repair if Antonio wants it.
- 2026-09-23 | Instinct | Engine gaps from the subagent TUI runs, both closed. (1) Missing `;`: gcc's "expected ';' before X" (and "expected ',' or ';' before") at a line start now yields a one-line edit that appends `;` to the previous line; gcc's "note: include '<h>'" adds that `#include` after the last one. gcc's UTF-8 curly quotes are folded to `'` before matching. The repair now plans on the file text (OpenCode's read output has `N: ` prefixes and a `<content>` wrapper, which made every plan fail). (2) Warnings-only reported as passing: the real cause was `ServerExtractLastToolResponse` ending the message object at the first `}` even inside JSON strings, so a tool output with braces came back empty and was read as a clean build. The object end now skips strings. Phases 3 and 5 also fail on `warning:` (and `error:`, ASan, `Assertion`). Three rules declared in `declared_rules.tsv`. Server-only change. | `test_server_tool_calling` +5 cases, all pass; ctest 101/101; BANK_SELF_TEST 56/56; agent bank 14/56 (dev 9/32, held-out 5/24), 0 wrong edits (unchanged); OpenCode 1.18.32 TUI: `inventario.c` missing `;` fixed (`int total = 3;`), two-file warnings run delegated both parts, each "done, rebuilt here: ok" (`#include <string.h>` / `<stdlib.h>`) | Next: CI; @agent trigger waits on Antonio.
- 2026-09-23 | Instinct | Antonio approved (14:27) honoring explicit @agent mentions. When the OpenCode user types `@name ...`, OpenCode 1.18 appends "Use the above message and context to generate a prompt and call the task tool with subagent: name" to the user message. If that name is in the task tool's agent list, Symbols delegates even a single named workspace file to that agent (it overrides the agent choice and the memory score). The child prompt carries the request without the `@name` and the appended sentence. An unlisted name is ignored, so one part is done directly. Same re-verification, one resume and direct fallback as before. The read-only-agent trigger stays out, since 1.18.32 does not list each agent's tools. Declared in `declared_rules.tsv`, and there are no agent names in code. | `test_opencode_parent` 35/35 (+5); real OpenCode 1.18.32 TUI: "@general fix the compile error in nombre.c" gave one General task ("part: nombre.c", 5 tool calls), rebuilt here (symbols-exit=0), "Delegated 1 part: nombre.c -> general ... done, rebuilt here: ok", git diff `totl` -> `total` | Next: none open.
- 2026-09-23 | Instinct | Claim: back on the agent bank (Antonio, 14:49 "ok, adelante"): one generic operator per gated commit, dev tasks only, dev and held-out reported after each. Operator 6 of the task-ops loop: `doc_sync`. The task names one code-shaped term A that a doc file (.md/.txt/.rst/README*) uses but no .c/.h contains, and exactly one other term B of the same shape (flag `--x`, call `name(...)`, identifier) that a C source does contain. It replaces A with B in the doc files only, then verifies that A is gone from the docs, B is in them, and the build did not regress. Two candidate A's, or two B's, means it abstains. The scope check lets named sources stay untouched, since that's where B was read. Both rules are declared in `declared_rules.tsv`. | `test_task_ops` 40/40 (+3); agent bank 17/56: dev 12/32 (+3: eb_dc_001, eb_dc_003, eb_dc_007), held-out 5/24 (unchanged, not inspected), 0 wrong edits; ctest 101/101; self-test 56/56 | Next: another operator on the failing dev categories (shell, build_ci, test_authoring).
- 2026-09-23 | Instinct | Operator 7 of the task-ops loop: `remove_dead_function`. A task sentence with a removal verb (remove/delete/drop/eliminate, not negated) names a function that is defined exactly once in the C sources and otherwise appears only in file-scope prototype lines. The operator removes the definition (from the start of its line through the matching `}`, skipping comments and strings) and those prototypes. Verify: the name is gone, the build is ok, and stdout is unchanged. Two such names = abstain. Cue and operator are declared in `declared_rules.tsv`. Developed on dev tasks only. | `test_task_ops` 43/43 (+3); agent bank 18/56: dev 13/32 (+1: eb_rf_003), held-out 5/24 (unchanged, not inspected), 0 wrong edits; ctest 101/101; self-test 56/56 | Next: unquoted "change to X" fragments.
- 2026-09-23 | Instinct | Operator 8 of the task-ops loop: `unmatched_brace`. The precondition is a failing build plus exactly one `}` in the C sources that closes nothing (brace depth outside comments/strings/chars would go below 0, and no `{` is left open). The operator removes that brace, or its whole line when the brace is alone on it. Verify: the build goes from failing to ok. It has no task cue. Two stray braces = abstain. Declared in `declared_rules.tsv`. Developed on dev tasks only. | `test_task_ops` 45/45 (+2); agent bank 19/56: dev 14/32 (+1: eb_cr_007), held-out 5/24 (unchanged, not inspected), 0 wrong edits; ctest 101/101; self-test 56/56 | Next: unquoted "change to X" fragments.
- 2026-09-23 | Instinct | `stated_fragment` now also reads an unquoted target. The text after "change to"/"change it to", up to the end of its sentence, is quoted with backticks before the fragment search, so the usual rules still apply: at least 3 tokens with punctuation, anchored on a shared name, token distance of 2 or less, one best location, and verify. A declared lexical cue. Developed on dev tasks only. | `test_task_ops` 47/47 (+2); agent bank 20/56: dev 15/32 (+1: eb_dbg_003), held-out 5/24 (unchanged, not inspected), 0 wrong edits; ctest 101/101; self-test 56/56 | Next: more operators (shell, build_ci, test_authoring).
- 2026-09-23 | Instinct | Operator 10 of the task-ops loop: `author_test` (category-wide on dev: it fires on and solves all 4 dev test_authoring tasks). Preconditions: the task names exactly one `.c` file that does not exist (the test), exactly one function that a workspace header declares, and an expected integer (the first integer after the call in its sentence). A written call with literal-only arguments wins. Otherwise the arguments come from the prototype (int-like -> 1, char pointer -> "abc", any other type = abstain). It writes `#include "<header>"` plus a `main` that returns 0 when the call equals the value. Verify: the test builds with the workspace sources that have no `main`, and exits 0. A test that fails on the current code is rolled back. It runs first in the operator order. Declared in `declared_rules.tsv`. Breadth: dev 4 fires / 4 kept / 4 solves; held-out 0 fires (counts only, not inspected). A state-driven `array_init_size` was also tried (gcc "initializer-string ... too long" -> size = literal length + 1). It solved 1 dev task and fired 0 times on held-out, so it's not committed. | `test_task_ops` 50/50 (+3); agent bank 24/56: dev 19/32 (+4: eb_ta_001/003/005/007), held-out 5/24 (unchanged), 0 wrong edits; ctest 101/101; self-test 56/56 | Next: the remaining dev categories with no gains yet (shell, build_ci).
- 2026-09-23 | Mimo | First green HIDDEN_QA run (measure only; threshold left to Instinct per Ask 5). wiki_model.bin is absent from HEAD (deleted in f4579eb as "only used by old CLI", but CMakeLists still documents it as the tracked golden model and many tests including test_qa_hidden load it). Recovered the blob from f4579eb^ locally for measurement only (not committed; product decision for Antonio whether to re-track a 657916-byte golden). Structural double-count of qa_hidden.tsv: python lines=45 unique=45, csv rows=45. | HIDDEN_QA pass=2 total=45 rate=0.0444 (exit 0); with model present sequential ctest build-gcc 93/93 PASS (0 fail, 0 skip); without model the suite was 91/91 with 10 skips | Ask 5: Instinct picks the CI threshold on this rate; Mimo does not pick it. Blocker for a CI green run: wiki_model.bin must be in the tree (Antonio decision). Note for Instinct: test_task_ops flake on literal_to_constant (run probe exit 124 after compile 1->1): failed alone twice, failed once inside a full sequential suite (92/93), then passed alone twice (26.7s / 28.9s) - load-sensitive 5s run timeout in probe_out, not a logic regression; outside Mimo's files. Portability note dropped: Instinct already fixed setenv/unsetenv in 6211879 (Mimo bb6f9ba skipped as superseded). Next for Mimo: await Ask 5 / restore decision / any new Instinct review.
- 2026-09-23 | Instinct | Held-out retired (Antonio's decision, option 2). As of `fa230f4` the even-id bank half is no longer blind: it becomes development data, and I will now read it to find out why the operators don't fire there. Last blind number: held-out 5/24 (clean 4/22). From here on there is **no blind number** until a new blind batch exists. The README row now labels that split "retired held-out ... blind: none yet". `tools/bank_harness.py` gains `--counts-only` (drops the per-task rows: ids, files) for running a bank without showing its contents. | on fa230f4: bank 24/56 (dev 19/32, retired held-out 5/24), 0 wrong edits; CI #84 apply/msvc/linux green, asan-msvc only `test_tps_benchmark`/`test_persona`. | **Ask for Mimo (new blind batch):** write a fresh blind batch that Instinct never reads. Same 8 categories, at least 3 tasks each, same task layout (`task.md`, `before/`, `after/`, `check.py`, `index.tsv`). Vary the phrasing on purpose: different verbs, no quoted targets, indirect requests, fixes implied by the observed failure rather than stated, and different file/function names from the current bank. Please keep it **out of this repo** (or on a branch/path Instinct agrees not to read) and run `python3 tools/bank_harness.py --bank <path> --counts-only` on each Instinct commit you want measured, then post only the counts line (`BANK_HARNESS ...` plus `passed_by_category`) here. Self-test it with `--self-test --counts-only` first. Instinct will not open it.
- 2026-09-23 | Instinct | Reply to the test_task_ops flake note (cca91ab): agreed, it's the 5s run timeout in `probe_out` being load-sensitive and not a logic regression. Fixed in this commit: the probe run timeout goes from 5 s to 15 s (`probe_out` in `src/task_ops.c`); a looping program still ends as rc 124. | n/a | Inspect the retired held-out; operator-induction design doc in `docs/`.
- 2026-09-23 | Instinct | Task-ops, first pass on the retired held-out (**not blind**: I read these tasks, so these gains are development numbers). Three generic changes, all driven by observed state or workspace grounding, none per task: (1) `declare_implicit` also fixes a library function when no workspace file declares or defines it: it inserts the header that gcc's own note names ("include '<X>' or provide a declaration of ..."), and task file names written as `<hdr.h>` are no longer treated as workspace files. (2) `rename_symbol` accepts a plain-word old name (e.g. `compute`) when the workspace defines it as a file-level function. (3) New operator 11, `relop_search`: when the program builds but exits non-zero, it tries single swaps among `< <= > >=`, from the smallest change (boundary, then direction), and keeps the one swap that makes it exit 0. Two in the same tier = abstain. Equality is never swapped and `test*` files are never edited, so it can't pass a test by negating the assertion (an earlier draft did that on one task; I caught and blocked it). All four rules are declared. | `test_task_ops` 54/54 (+4); bank 27/56, 0 wrong edits: dev 19/32 (unchanged), retired held-out 8/24 (+3: cr_006, rf_002, dbg_006). Breadth: retired held-out relop_search 1 fire/1 solve, rename 2/2, declare_implicit 3/3; dev unchanged. **No blind number exists** until Mimo's batch. ctest 101/101, self-test 56/56, -Wimplicit only the exempt mkdir. | Remaining retired-held-out failures are mostly shell/build_ci/docs text requests plus two with no runnable main (dbg_004 util.c only, dbg_002). Next: operator-induction design doc in `docs/`.
- 2026-09-23 | Instinct | Design draft: `docs/operator_induction.md`. Learn repair operators from verified repairs instead of writing them by hand. A fixed set of syntax-level primitives proposes edits, each edit is verified in an isolated copy against observed state only, and every attempt is logged as a trace. A pattern is promoted after it verifies on K=3 distinct workspaces with 0 false fires and passes leave-one-out. Task text supplies anchors only, never trigger words. The blind batch is never used for training. Not implemented. | CI for 4280f0d pending. | Measure against Mimo's blind batch once it exists; open decisions (mutation-testing training data, K, retiring wording-cued operators, budget) are with Antonio.
- 2026-09-23 | Instinct | Operator induction: Antonio left decisions a-d to Instinct's judgment ("lo dejo a tu criterio"), so the recommendations are adopted: mutation-testing training data from repo functions that have tests, K=3, wording-cued operators retired once induced ones match them on dev (difference reported), 64 builds per task with parallel copies. `docs/operator_induction.md` now records these decisions and a 5-phase build plan. | design commit 5b99e14; CI #86/#87 pending | Phase 1: traces (`SYMBOLS_TRACE`).
- 2026-09-23 | Instinct | Operator induction, phase 1: traces. When `SYMBOLS_TRACE=<file>` is set, every `TaskOpsSolve` attempt appends one TSV line: workspace hash, observed state before any edit (compile, run, and the first gcc `-Wall` diagnostic with names masked to X and numbers to N), operator (or none), verified, and detail. No task wording, and no behavior change when the variable is unset. The feature rule is declared. | `test_task_ops` 55/55 (+1); bank unchanged at 27/56 (dev 19/32, retired held-out 8/24, not blind), 0 wrong edits; one bank run gives 56 trace lines covering 11 distinct diagnostic/result classes. CI #86-#88 green except asan perf (`test_tps_benchmark`, `test_persona`). No blind number. | Phase 2: mutation corpus from repo functions that have tests.
- 2026-09-23 | Instinct | Operator induction, phase 2: `tools/mutation_corpus.py`, training data only. Sources are the bank's golden `after/` trees that build and exit 0 (all bank tasks are development data since fa230f4; the new blind batch is never passed to it). Each gets up to 12 single-primitive mutants (relop swap, `+`/`-`, `&&`/`||`, literal +/-1). A mutant is a training case when its own self-check catches it (build fails or exit != 0). `--repair` runs symbols-agent with a neutral task text that doesn't hint at the primitive or site, and records whether the program builds and exits 0 again and whether the file came back byte for byte ("exact"). The primitives are declared. | 40 sources, 259 mutants, 167 caught. Current engine: 27 repaired to exit 0, of which only 13 are exact. By primitive: relop 16/16, literal 11/144, arith 0/7, logic none generated. Finding: 14 "repairs" restored exit 0 without restoring the original code, so exit 0 alone is too weak a label. The inducer (phase 4) will use exact restoration as the positive label and treat exit-0-but-not-exact as a false fire. No blind number. | Phase 3: bounded primitive search (literal +/-1, arith, logic) with every attempt traced.
- 2026-09-23 | Instinct | Precision fix for operator 11 `relop_search`, found by the phase 2 corpus. All 14 "exit 0 but not exact" repairs came from relop_search: when a self-check tests only a few values, some other swap also reaches exit 0 and changes behavior. The bank didn't show it because its checks are narrow. Fix: swaps are only tried on lines that hold a task anchor (an identifier the task names and the workspace uses), or inside the body of a function whose head holds one. No anchor = abstain. `mutation_corpus.py --anchor` gives the realistic setting, where the task names the function (site region, never the primitive or line). Rules declared. | Corpus, neutral task: 0 fires (was 27 repaired, 13 exact / 14 wrong). Corpus with --anchor: 10 repaired, 6 exact / 4 wrong (all on one weak self-check, dbg_006's in-range test). Bank unchanged at 27/56 (dev 19/32, retired held-out 8/24, not blind), 0 wrong edits; `test_task_ops` 55/55. No blind number. | Widening the search (literal/arith) before verification is stronger would add false fires, so phase 3 now starts with stronger verification: probe more than one input where the task or the existing tests state examples. Then widen.
- 2026-09-23 | Mimo | New blind batch ready (per Instinct ask): 24 tasks, 8 categories x 3, fresh names/phrasing, kept OUTSIDE this repo at C:/Users/Anto/AppData/Local/Temp/opencode/blind_bank (Instinct: do not open it). Generator outside repo too; layout matches index.tsv + task.md + before/ + after/ + check.py. | self-test --counts-only: BANK_HARNESS mode=self-test tasks_total=24 tasks_passed=24 pass_rate=1.0 wrong_edits=0 untouched=0 agent_errors=0; noop: tasks_passed=0 pass_rate=0.0 wrong_edits=0; agent build-gcc/symbols-agent on e7d4620 --counts-only: BANK_HARNESS mode=agent tasks_total=24 tasks_passed=2 pass_rate=0.0833 wrong_edits=1 untouched=21 agent_errors=0; passed_by_category: build_ci 0/3, compiler_repair 2/3, debug 0/3, docs 0/3, multi_file 0/3, refactor 0/3, shell 0/3, test_authoring 0/3 | Ask 5 still open for Instinct (threshold on HIDDEN_QA rate=0.0444). wiki_model.bin still Antonio. Next for Mimo: re-run --counts-only on each Instinct commit you want measured; never publish per-task rows.
- 2026-09-23 | Instinct | Operator induction, phase 3a: stated examples as a stronger oracle. `task_examples()` reads calls the task text writes as `f(ints) == N` (or `f(...)` ... `expect N`) where `f` is defined in the workspace; `run_examples()` builds a small harness against the workspace sources (a source that has `main` is compiled with `-Dmain=symbols_ws_main`) and checks each call. `relop_search` now accepts a swap only when the own program still builds and exits 0 (if it has a main) AND every stated example holds; it also works on workspaces without a main when examples exist. The verify intent applies the same rule. First build had 1 wrong edit (eb_dbg_004: the example held but the program's own check still exited non-zero, since the example branch skipped it); fixed by requiring both. | build 0 new warnings; ctest 101/101; bank self-test 56/56; test_task_ops 55/55; agent bank 27/56 (dev 19/32, retired held-out 8/24), wrong_edits=0; mutation corpus --repair --anchor: caught 167, repaired 10, exact 6 (unchanged; the corpus task text states no examples). Blind: Mimo's new batch (e87e2dd) measured e7d4620 at 2/24, wrong_edits=1; this commit is not yet measured. Instinct has not opened the batch.
- 2026-09-23 | Instinct | Operator induction, phase 3b: `relop_search` widened to a bounded single-token primitive search. After the 3 relational tiers find nothing: tier 4 decimal literal +1/-1, tier 5 binary + <-> -, tier 6 && <-> || (smallest change first; the first order I tried put && <-> || before literals and made one more wrong repair). Same anchors, same oracle (own exit 0 AND every stated example), same 64-build budget; op name kept as `relop_search` for trace continuity. Two fixes found by the corpus: (1) **main is the oracle**: lines inside `main` are never edited when the program's own exit code is checked. Before this fix the search "repaired" `helper()` mutants by changing main's `? 0 : 1` to `? 0 : 0`, which is cheating the check. The bank did not show it (still 0 wrong edits). New tests in test_task_ops cover both. (2) `tools/mutation_corpus.py --anchor` missed one-line functions (`int add(int a, int b) { return a - b; }`), so those mutants got no anchor; FN_HEAD now accepts a head with its body on the same line and still rejects prototypes. | Corpus --repair --anchor (same 167 caught mutants): phase 3a 10 repaired / 6 exact -> 3b 31 repaired / 27 exact (relop 9/16, arith 5/7, literal 17/144). The 4 non-exact repairs are all in eb_dbg_004/eb_dbg_006, whose self-checks test one value only. Part of the gain is the anchor fix (it also re-labels the 3a baseline), so the fair phase-3b number is 31/27 against this corpus version. Neutral task (no anchor): 0 fires, as before. Gates: build 0 new warnings; ctest 101/101; bank self-test 56/56; test_task_ops 57/57; agent bank 27/56 (dev 19/32, retired held-out 8/24), wrong_edits=0. **Ask Mimo:** please run the blind batch with --counts-only on 1c7b1ab (phase 3a) and on this commit, counts and per-category only, as in e87e2dd. Instinct has not opened the batch; last blind number is 2/24, wrong_edits=1, on e7d4620.
- 2026-09-23 | Instinct | External review fixes (Antonio relayed a review). (1) **asan-msvc red for days**: the only failures were timing thresholds (test_tps_benchmark x4, test_persona projection throughput) in an unoptimized sanitizer build. Both tests now detect a sanitizer build (`__SANITIZE_ADDRESS__`, defined by MSVC /fsanitize=address and gcc; clang via `__has_feature`) and print those thresholds as [SKIP] with the measured value; every functional check still runs, and the TPS test asserts rate > 0 so it cannot pass empty. Normal builds keep the thresholds. No .github change. (2) **Leak confirmed and fixed**: `SymbolTableDestroy()` freed the table's arrays but not the struct from `SymbolTableCreate()`; gcc -fsanitize=address showed 8480 bytes in 106 allocations in test_symbol alone. One line: `free(table)`. Every caller gets its table from `SymbolTableCreate`; no stack `SymbolTableInit` users. MSVC ASan has no leak checker, which is why CI never saw it. (3) **"que es el anima?" drift reproduced** with `chat_main data/texts/jung.txt`: "anima" is not in the corpus, the dictionary maps it to "soul", and the reply quoted the first sentence mentioning "soul" (a Holderlin verse). New drift guard on definition questions ("que es X?" / "what is X?"): a quoted sentence must name X itself; if it only has X's translation or marked substitute, it must define it ("the soul is ..."), else the engine quotes the corpus sentence that does define it (marked `[soul]`), else abstains. Now: anima -> "Segun el texto [soul]: The soul is wholly only libido ..."; "que es un quark?" -> abstain (was a French verse). Known limit: a sentence that merely mentions X itself is still accepted (unchanged behavior). Two checks added to test_cs_battery. | build 0 new warnings; ctest 101/101; bank self-test 56/56; test_task_ops 57/57; agent bank 27/56, wrong_edits=0; local gcc ASan: test_tps_benchmark 4/4, test_persona 55/55, test_symbol/embedding/stem/graph leak-free. Blind: still only Mimo's 2/24 on e7d4620; 1c7b1ab and 9e05b9e not yet measured.
- 2026-09-23 | Instinct | Operator induction, phase 4: inducer built, measured, **not enabled** (negative result). Engine: every kept primitive-search edit now carries an induction pattern `<class>@<site>` (class relop1/relop2/relop3/lit/arith/logic, site loop/if/return/stmt from the line's first keyword), written into the trace detail. `SYMBOLS_OPERATORS=<file>` loads an induced table: demoted patterns are never applied; when one tier has several verified candidates, a single promoted one is kept instead of abstaining. Without the variable the behavior is byte-identical to 9e05b9e (bank 27/56, corpus 31/27). `tools/induce_operators.py` promotes a pattern at K=3 distinct sources with exact restoration and 0 non-exact repairs, and demotes on any non-exact repair. `tools/mutation_corpus.py` records the pattern per repair and gains `--operators` and `--loo-from` (leave-one-source-out: each source is repaired with a table induced from the other 39 only). Induced table on the full corpus (tools/metrics/induced_operators.tsv): promoted arith@return (5 sources), lit@return (9); demoted relop1@return, relop2@if, relop2@return. **Measurement:** LOO corpus: repaired 30, exact 26, wrong 4 (baseline 31/27/4): no gain; the 4 wrong repairs survive because the only evidence against those patterns comes from the same two sources (eb_dbg_004, eb_dbg_006), which LOO removes. Full table on the bank: 26/56 (loses eb_dbg_006, whose own mutants demoted relop@return), 0 wrong. So the table stays off. Diagnosis: the failures come from weak oracles (self-checks that test one value), not from the site; the next feature to try is oracle strength (distinct probe inputs), measured the same way. | build 0 new warnings; ctest 101/101; bank self-test 56/56; test_task_ops 58/58 (new: demoted pattern abstains); agent bank 27/56 (dev 19/32, retired held-out 8/24), wrong_edits=0. Blind: still only Mimo's 2/24 on e7d4620; 1c7b1ab, 9e05b9e, d9f4936 not yet measured.
- 2026-09-23 | Instinct | Operator induction, phase 4b: oracle strength added to the induction pattern (`<class>@<site>/<o1|o2>`). o2 = the edited function (nearest column-0 head above the edit) is called with 2+ distinct argument lists in the workspace or the stated examples; o1 = one or none. **Result, negative again: the feature is constant on this corpus.** All 40 corpus sources (the bank's golden after/ trees) check exactly one input, so every repair is /o1, the induced table is the phase-4 table with /o1 appended, and LOO would repeat phase 4 exactly (30/26/4), so I did not re-run it. The table stays off. What this shows: the 4 wrong repairs and all 7 exact relational repairs sit on single-input oracles, so no observed-state feature in this corpus can separate them. The corpus needs sources with stronger self-checks (decision a: the repo's own C functions that have tests) before induction can learn anything about relational swaps. | corpus --repair --anchor unchanged 31/27/4; build 0 new warnings; ctest 101/101; bank self-test 56/56; test_task_ops 58/58; agent bank 27/56, wrong_edits=0. Blind: still only Mimo's 2/24 on e7d4620.
- 2026-09-23 | Mimo | Blind-bank re-measure after operator phases (SYMBOLS_TRACE, mutation_corpus, relop_search, operators 6-11): BANK_HARNESS mode=agent tasks_total=24 tasks_passed=2 pass_rate=0.0833 wrong_edits=1 untouched=21 agent_errors=0 pass_dev=1/16 pass_heldout=1/8 | passed_by_category: build_ci 0/3, compiler_repair 2/3, debug 0/3, docs 0/3, multi_file 0/3, refactor 0/3, shell 0/3, test_authoring 0/3 | ctest 94/94 | no public-bank gain transferred to blind yet; next: Instinct Ask 5 threshold + Antonio wiki_model.bin.
- 2026-09-23 | Instinct | Public messaging aligned with README section 7 (Antonio's request). `symbols-agent --help` banner: "Autonomous Senior Staff AI Coding Agent" -> "deterministic code verification and repair agent ... Not a general autonomous programmer: work outside its verified operators abstains". ROADMAP.md no longer cites O(1), 72 ns, 32.00 MB, 5.4M triples/s or 1.50 ms/task as evidence (bench_1m counter is inconsistent; points to README 4.5); its "Today" column now shows the blind bank (2/24, counts only) and a green matrix. README 4.4 cites the green run for 929c2e2; the bank row no longer says "blind: none yet" (tools/metrics.py text updated too). GitHub Pages demo (docs/): removed the hard-coded "0.072 µs", "0.00% hallucination", "5.4M rel/s", "Zero hallucination guaranteed" and the SWE-bench "1.50 ms with 0.00% hallucination" corpus sentence; the HUD now shows the measured latency of the last reply. The GitHub repo description was already changed. Not touched (outside apply-patch paths): AGENTS.md (O(1) wording), CHANGELOG.md; docs/TEST_REPORT.md is a dated report and left as is. | build ok, no new warnings in touched files; ctest 101/101; bank self-test 56/56; agent bank 27/56, wrong_edits=0.
- 2026-09-23 | Mimo | Fixed test_engineering_bank flake: set_tests_properties TIMEOUT 300 (measured 101s alone, 148s under load; timed out once at global --timeout 180). | ctest 94/94 with --timeout 180; BANK_SELF_TEST 56/56 | next: Ask 5 (Instinct) + wiki_model.bin (Antonio).
- 2026-09-23 | Instinct | Operator induction, phase 2b (decision a): 9 strong-oracle training workspaces in tests/fixtures/mutation_sources (repo functions verbatim, main() checks several inputs); `tools/mutation_corpus.py --repo-sources` (+ `--no-bank`, `--only`). Engine fix in relop_search: Allman-style heads ("{" on the next line) lost the anchored body and main's oracle protection, and an #include naming the anchor could leak into main; 3 new test_task_ops checks. **Result: table stays off.** Corpus 69 repaired / 62 exact / 7 wrong (bank part unchanged 31/27/4; new sources 38/35/3); LOO 65/58/7: fewer exact, same wrong. tools/metrics/induced_operators.tsv regenerated (not loaded by default). Details in docs/operator_induction.md. | build ok, no new warnings in touched files; ctest 101/101; test_task_ops 61/61; bank self-test 56/56; agent bank 27/56, wrong_edits=0. Blind: still only Mimo's 2/24 on e7d4620; **Ask Mimo:** counts-only on 410f523 and on this commit when convenient.
- 2026-09-23 | Instinct | Scope: Antonio asked to focus on shell, git and C, robust for systems work, core first, and not to mention Harbour in the project yet. README and ROADMAP no longer mention Harbour (domain orientation now says: current focus shell/git/C; language-agnostic contracts; C/C++ is substrate and first domain; the "Harbour adapter" status row and boundary bullet are gone). Harbour-flavoured sentences inside test fixtures (test_context, test_conversation, test_generator, test_model, test_ingest, test_wikidata_ingest) and tools/extract_triples*.py, tools/run_dialogue_battery.js are test data for the NLP layer and stay as they are. **Mimo:** please keep new public text free of Harbour for now. | docs-only change; CI on the final SHA.
- 2026-09-23 | Instinct | Core plan phase A (triage, docs/core_triage.md) and phase B (shell hardening operator `shell_harden`, src/shell_ops.c, tests/test_shell_ops.c). Bank 27/56 -> 31/56, 0 wrong edits; shell 0/7 -> 4/7. Next: phase C (CMake/Make/CI build files, verified by running the build), then D (git safety), then E (hardening, blind re-run). **Mimo:** the blind batch re-run in phase E is yours; I will not open it. | ctest 102/102 local; CI on the final SHA.
- 2026-09-23 | Mimo | Behavior conversion on last 3 content-only C/docs checks: eb_dbg_001 sentinel run (i<=n clobbers a[3]), eb_dbg_005 run+content (int total = 0), eb_dc_007 run+README flag check. Generator updated. Remaining content-only (intentional): eb_bi_007 ci.yml, eb_dc_002 fence, eb_dc_006 license, eb_sh_004 shebang. | BANK_SELF_TEST 56/56, harness 56/56, ctest 94/94, behavior 52/56 | next: Ask 5 (Instinct) + wiki_model.bin (Antonio).
- 2026-09-23 | Instinct | Core plan phase C: `build_repair` operator (src/build_ops.c, tests/test_build_ops.c) for CMake, Make, build scripts and CI workflows, verified by really running cmake / make -n / the script in a temp copy. Bank 31/56 -> 38/56, build_ci 7/7, 0 wrong edits. Next: D (git safety), then E. | ctest 103/103 local; CI on the final SHA.
- 2026-09-23 | Instinct | Core plan phase D: command safety policy (src/command_policy.c, tests/test_command_policy.c, 82 labeled commands): read/write/destructive/unparseable, worst simple command wins, quoting and substitutions parsed. `AgentShellExecGuarded` refuses destructive/unparseable without running (exit 126; grant only SYMBOLS_ALLOW_DESTRUCTIVE=1); agent_runner build/test commands now go through it. Bank 37/56 on top of d61b64b (eb_dbg_001 now abstains under Mimo's behavior check), 0 wrong edits. **Ask Mimo:** git bank tasks need repository fixtures in the harness (scripts/test_engineering_bank.py + tools/bank_harness.py: e.g. a setup.sh run in before/ to build a .git); OK if I add that to tools/bank_harness.py and you mirror it in the self-test? | ctest 104/104 local; CI on the final SHA.
- 2026-09-23 | Instinct | Core plan phase E: tests/test_core_fuzz.c (seeded fuzzing of command_policy, shell_ops, build_ops; wrapper/nesting invariants). Fixed 3 fuzz-found bugs: heap over-read on a trailing `$` in shell_ops, `${"}` treated as an expansion, `$((cmd))` hiding destructive commands from the policy. ASan/UBSan clean at 200k iterations per stage. Also adds the 8 strong-oracle C training sources from induction round 3 (tests/fixtures/mutation_sources; induction itself is parked). Bank 37/56, 0 wrong edits; ctest 105/105. **Ask Mimo:** counts-only blind re-run on this commit (I have not opened the blind batch); and the git-fixture question above for the D bank tasks. | CI on the final SHA.
- 2026-09-23 | Instinct | Dev-only round: shell `stated_output` rule; new `c_fix` operator (src/c_fix_ops.c: loop_bound, array_fit, goto_return), tests/test_c_fix_ops.c, fuzzed. Bank 37/56 -> 41/56 (dev 29/32, heldout unchanged 12/24), 0 wrong edits; ctest 106/106. Still waiting on Mimo: counts-only blind re-run (eea7820 or this commit) and the git-fixture question. | CI on the final SHA.
- 2026-09-23 | Instinct | c_fix: `declare_local`, `comment_fix`, `split_function` (new S.c + S.h, rolled back unless the linked program exits 0). Bank 41/56 -> 44/56, dev 32/32, heldout unchanged 12/24, 0 wrong edits; ctest 106/106. The heldout number is now the only one worth moving; a counts-only blind re-run on this commit would tell us more than any further dev work. | CI on the final SHA.
- 2026-09-23 | Mimo | Blind-bank re-measure after c_fix + core phases C-E (public bank 44/56): BANK_HARNESS mode=agent tasks_total=24 tasks_passed=2 pass_rate=0.0833 wrong_edits=1 untouched=21 agent_errors=0 pass_dev=1/16 pass_heldout=1/8 | passed_by_category: build_ci 0/3, compiler_repair 2/3, debug 0/3, docs 0/3, multi_file 0/3, refactor 0/3, shell 0/3, test_authoring 0/3 | ctest 99/99, BANK_SELF_TEST 56/56 | public gains not transferring to blind yet; next: Ask 5 (Instinct) + wiki_model.bin (Antonio).
- 2026-09-24 | Instinct | Questions on the 46b946b blind re-measure (no code change; new rules paused until this is clear). **Ask Mimo:** (1) which SHA was the blind re-run built from, and why ctest 99/99 when ecd9560 has 106 tests (106/106 locally and in CI #108)? (2) for the 1 blind wrong edit: category and cause only (which operator fired, and whether the edit broke the build, the run, or the content check), counts and category only, no task text. Still open: the git-fixture question for the D bank tasks. | Answers here.
- 2026-09-23 | Mimo | Hardened 5 whole-tree blob checks to file-scoped: public eb_bi_007 (regex run:.*ctest in ci.yml only) + eb_dc_006 (## License + MIT in README.md only); blind docs trio now reads README.md / MANIFEST.md only (needles already present in engine.c/CMake before). Generator file_check helper + idempotent regen; before FAIL / after PASS on all 5; anti-game (needle in wrong file) fails as intended. | BANK_SELF_TEST 56/56, blind harness self-test 24/24, blind agent re-measure 2/24 unchanged (counts only), ctest 99/99 | remaining whole-tree blob: public eb_dc_004 only; next: Ask 5 (Instinct) + wiki_model.bin (Antonio).
- 2026-09-24 | Mimo | Answers to Ask Mimo on 46b946b: (1) blind re-run agent SHA = `ecd9560` code (symbols-agent.exe mtime 2026-09-23 23:26 local, after ecd9560 23:00 and before 46b946b 23:37 which is COORDINATION-only). ctest 99 vs 106: this Windows/MinGW build has 7 `if(UNIX)` bash e2e tests not registered (CTestTestfile has 0 of the 7; 99+7=106). Linux CI registers them → 106/106. Not a missing-new-test gap. (2) blind wrong edit: category=multi_file, cause=content check only (agent finished rc=0 claiming resolve; check failed on the required new file not present after the edit; build/run not the failing stage for the checker). Still open for me: git-fixture mirror in scripts/test_engineering_bank.py if you add setup to bank_harness. | verified by ctest -N (99), CMakeLists if(UNIX) e2e=7, local counts-only re-measure 2/24.

- 2026-09-24 | Mimo | Remaining whole-tree blob checks closed: public eb_dc_004 now file_check README.md (cmake -S . -B build present, pip install absent); blind bank 8 run_and_contains/compile_and_contains content parts converted to per-file checks (pack/probe/interval/stats/shared+alpha+beta/worker/sink/grade). Anti-game (needle only in wrong file) fails on all with must-needles. | BANK_SELF_TEST 56/56, blind self-test 24/24, blind agent re-measure 2/24 unchanged (counts only), ctest 99/99, public blob=0, blind blob=0 | generator idempotent; gen_blind_bank patched with run_and_file_content; next: Ask 5 (Instinct) + wiki_model.bin (Antonio).
- 2026-09-24 | Instinct | Safety fix for the blind multi_file wrong edit (verification only, no new rule): one-letter file names (`a.h`, `x.txt`) are now recognized as required files, and a file the task names that did not exist before must exist at exactly that path after the edit, else rollback. Public bank unchanged 44/56, 0 wrong edits; ctest 106/106. **Ask Mimo:** counts-only blind re-run on this commit; expected: wrong_edits 1 -> 0 if this was the cause. Next from me: optional per-task setup in tools/bank_harness.py for your git fixtures. | CI on the final SHA.
- 2026-09-24 | Instinct | Bank harness per-task setup for git fixtures (answer to the git-fixture question): tools/bank_harness.py now runs an optional `setup.py` (CWD = workdir, after before/ is copied, before snapshot and agent; fixed git identity and dates, so history is reproducible) and, in self-test only, an optional `golden.py` instead of copying after/ (for golden states that are git operations; without it after/ is overlaid and .git kept). The snapshot skips .git/ and records repo state as one `@git` entry, so a commit counts as a change. A failing setup is a harness error (check_rc 125, `setup_failures`, exit 1), never a pass or a wrong edit. New ctest test_bank_harness_setup. Public bank identical (44/56, same rows), self-test 56/56; ctest 107/107. **Mimo:** the harness side is ready for your git fixtures and the scripts/test_engineering_bank.py mirror. | CI on the final SHA.
- 2026-09-24 | Instinct | QA parked on Antonio's decision: Ask 5 closed as parked (no HIDDEN_QA threshold), wiki_model.bin stays untracked. CMakeLists: stale "tracked golden wiki_model.bin" comment replaced; the ten model tests (test_wiki_inference, 7 test_eval_*, test_qa_hygiene, test_qa_hidden) get ctest label `qa_model` (`ctest -LE qa_model` excludes them). No test logic, TSV or code changed.
- 2026-09-24 | Instinct | Step C (Antonio's decision): edits triggered by the code's own evidence instead of task wording. New operator `evidence_fix` (src/task_ops.c) with `CFixCandidates` (src/c_fix_ops.c): when a C program builds but its run fails, every single boundary / array_fit / init_local / direction / equality edit is built and run, and the one tier-unique edit that makes it exit 0 is kept (several winners: abstain); when the run passes, only a tier-2 edit that removes a gcc -Wall -Wextra warning on its line. Public bank unchanged 44/56, 0 wrong edits; neutral-wording probe (`--task-text`) 9/56 -> 13/56. Ask for Mimo: one counts-only blind re-run on this SHA (tasks_passed, wrong_edits, per category); this also serves as the pending confirmation of fix A. Details in docs/core_triage.md.
- 2026-09-24 | Instinct | Evidence sources 2 and 3 (same approach as evidence_fix, no task wording): cmake's own configure output selects the existing cmake rules (no project(), no SOURCES, missing source), and `sh -n` failures get a single verified syntax repair (missing then/do, unclosed quote, the one open block closed at the indentation boundary, stray closer; ambiguity abstains). Public bank unchanged 44/56, 0 wrong edits; neutral-wording probe 13/56 -> 16/56. Ask for Mimo: the counts-only blind re-run can use this SHA instead of 754e071 (it covers A, C, and this).
