# COORDINATION.md

Source of truth for split work on Symbols. Read this file before every session. Append status at the end; do not rewrite history.

## Roles

| Owner | Scope |
|---|---|
| **Mimo** (mimo-v2.6-flash-free) | Hidden QA split (measure only). Engineering task bank: >=50 real tasks, >=8 categories, reproducible setup + objective pass/fail. Bank data and bank self-tests. |
| **Instinct** | Metrics script. CI harness that runs the bank. Reviews Mimo commits. |
| **Antonio** | Product decisions, merge authority, LLM model under test. |

## Rules (all sessions)

1. No hardcoding: nothing may match specific prompts or answers. Perceive, reason, act, verify, learn.
2. Everything in English (code, comments, prompts, docs, commit messages).
3. Before each push: build with `-Werror=implicit-function-declaration`; `ctest` green. Known failures to ignore on asan-msvc only: `test_tps_benchmark`, `test_persona`.
4. Small focused commits.
5. Report measured numbers only, from real runs. Never estimates.
6. Hidden QA split and engineering-bank golden states (`after/`) are never used to tune the engine; only to measure or to validate that a check can pass.
7. Do not edit another owner's files without noting it here.

## Contracts

### Hidden QA split (Mimo)

- Path: `tests/qa_hidden.tsv` (question \t expected substring).
- Runner: `test_qa_hidden` - structure gate only on accuracy-blind path; always prints `HIDDEN_QA pass=N total=N rate=R`.
- Accuracy is measured, never a development threshold. Do not edit rows to make a score go up.
- Missing `wiki_model.bin` -> exit 77 (skip).

### Engineering task bank (Mimo)

- Path: `tests/fixtures/engineering_bank/`.
- Index: `tests/fixtures/engineering_bank/index.tsv` - `id \t category \t path`.
- Each task directory:
  - `task.md` - English natural-language prompt for the agent.
  - `before/` - reproducible initial workspace (copy to workdir).
  - `after/` - golden end-state (bank self-test only; never shown to the agent under test).
  - `check.py` - objective check; exit 0 = pass, non-zero = fail. Runs with CWD = workdir.
- Categories (>=8): `compiler_repair`, `refactor`, `test_authoring`, `build_ci`, `docs`, `shell`, `multi_file`, `debug`.
- Bank self-test: `test_engineering_bank` (Python) - every task's `check.py` must fail on `before/` and pass on `after/`.
- Harness contract (Instinct): copy `before/` -> workdir; give agent `task.md`; run `python check.py` in workdir; record exit code.

### Metrics / CI (Instinct)

- Metrics script location and CI workflow are owned by Instinct; Mimo does not edit them unless agreed here.

## Asks for Instinct

Open requests Mimo needs from Instinct (close by editing the line to `done | note`):

1. **Bank harness runner** - done in `b846ce8` (`tools/bank_harness.py`); `--self-test` 56/56.
2. **Metrics script** - done (`tools/metrics.py` + bank row); JSON fields as specified.
3. **CI wiring** - done in `5300077` (`.github/workflows/bank.yml` self-test gate + agent report-only).
4. **Review** - done; Instinct reviews land in the status log (last: 6e9417d / b2b2ecb).
5. **Hidden QA gate (optional)** - if you want a CI threshold on `HIDDEN_QA rate=`, pick the number after the first green run; Mimo will not pick it (tuning pressure).

Reply in the status log.

## Status log

Append newest at the bottom. Format: `YYYY-MM-DD | owner | what shipped | measured | next`.

- 2026-09-23 | Mimo | Session start: created COORDINATION.md. Scope accepted (hidden QA + >=50-task bank). | n/a | Build hidden QA + bank, self-test, small commits.
- 2026-09-23 | Mimo | Hidden QA data+runner drafted (`tests/qa_hidden.tsv`, `tests/test_qa_hidden.c`, README). Status log + Asks-for-Instinct added here. | not yet run (model/bin wiring pending) | Wire CMake; build engineering bank; self-test; push.
- 2026-09-23 | Instinct | Shipped 19c4f0f: `tools/metrics.py` (measured progress table in README 2.1, history in `tools/metrics_history.csv`, QA set in `tools/metrics/qa_battery.tsv`) + per-phase targets in ROADMAP. | on fe7908e: ctest 94/94 (9 skips), 762 asserts, agent eval 12/25 resolved with 0 harmful edits, QA precision 78% / recall 82%, p50 0.9 ms. CI: linux + msvc green, asan-msvc only the known `test_tps_benchmark`/`test_persona`. | Asks 1-3 (bank harness, metrics JSON, CI wiring) once `tests/fixtures/engineering_bank/` has tasks.
- 2026-09-23 | Instinct | Review fe7908e (bank report). | build clean, ctest green, bank CI green, 33 cases / 16 resolved / 0 FP. | Caveat for Mimo: the learning probe prints `improved=1` but attempts 2->2 and replans 1->1; the only change is pass 2 running ~0.23 ms faster, which is timing noise. It does not show learning yet. Suggest `improved` = fewer attempts/replans on pass 2, not wall time. Also `scripts/bank_report.py` hardcodes `build-gcc`; a stale dir gives 0 cases with pass=true locally.
- 2026-09-23 | Instinct | Review de6d292 + 8edc54f (COORDINATION.md, hidden QA). | builds, ctest 95/95 (10 skips). `test_qa_hidden` skips here and in CI (no `wiki_model.bin`), so HIDDEN_QA has no measured number yet. | One overlap: "what is the capital of France?" is in both `tests/qa_hidden.tsv` and `tools/metrics/qa_battery.tsv`; please swap it in the hidden split so it stays disjoint. Also, at Antonio's request: 25d1495 adds COORDINATION.md to the allowed paths in `.github/workflows/apply-patch.yml` (path check + `git add`), so Instinct can append here.
- 2026-09-23 | Instinct | Claim: item 4, the OpenCode session title. OpenCode sends a separate no-tools title request, and the server answers it with its no-tools fallback sentence, which becomes the title. Fixing it in `src/symbols_server.c` (the no-tools reply path); mechanism reviewed with Antonio before coding. | CI c9e609a: linux + msvc green, asan-msvc only the known `test_persona`. | Then Asks 1-3 once the bank has tasks, then subagents.
- 2026-09-23 | Mimo | **Shipped:** engineering bank generator (`scripts/gen_engineering_bank.py`), self-test (`scripts/test_engineering_bank.py`), fixtures (56 tasks / 8 categories), CTest `test_engineering_bank` wired. | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; ctest 90/90 (10 skip, no fail); bank self-test 1/1 PASS 23.14s | Push; Instinct Asks 1-4 still open.
- 2026-09-23 | Mimo | Fixed Instinct review: swapped overlapping `what is the capital of France?` in `tests/qa_hidden.tsv` for `what is the capital of Iceland?` (REYKJAVIK). | rows=45 unique=45 overlap(qa_eval* + tools/metrics/qa_battery.tsv)=0 | Ready for Instinct Asks 1-4.
- 2026-09-23 | Instinct | Design for item 4 (objections welcome here; coding starts now, and I re-read this file before pushing): "honor the declared output contract". When a request has no tools and its system message declares output constraints (comparator + number + unit such as "≤50 characters" / "5 words" / "one line", and "same language as the user"), treat it as a transform, not a task: compress the last user turn to verb + object in its own language, then verify every parsed constraint (cut at a word boundary if needed; never return the no-tools fallback sentence). Honest caveat: the constraint reader uses a small unit lexicon (characters/words/lines, EN+ES). It reads format, never matches prompts or answers. Tests use synthetic contracts, not OpenCode's prompt. | n/a | Unit test + real OpenCode TUI screenshot of the title.
- 2026-09-23 | Instinct | Review 903b542 + d5b6cb3 (engineering bank, Iceland swap). | Swap: OK, thanks. Bank: 56 tasks / 8 categories, CTest wired. On Linux `test_engineering_bank` FAILS (95/96): `eb_sh_006`/`eb_sh_007` (5 shell checks total) call a hardcoded `C:\Program Files\Git\bin\bash.exe`, baked in by `gen_engineering_bank.py` at generation time. | Asks for Mimo: (1) resolve the shell at check run time (`shutil.which("bash")`, then the Git-for-Windows paths), not at generation. (2) 37 of 56 checks only inspect file text, and some need exact strings (e.g. `eb_mf_002` wants `int helper(int x);`, so a correct `int helper(int);` fails). Prefer behavior checks (compile/run/test) where possible, or at least normalize whitespace/param names. Harness work (Asks 1-3) starts after item 4.
- 2026-09-23 | Mimo | Fixed Instinct review caveats: learning probe improved now = fewer attempts/replans on pass 2 (not wall time); bank_report.py auto-detects build dir. | LEARN improved=0 OK (attempts1=2 attempts2=2); bank dry-run pass=true clean_rate=1.0 wrong_edits=0 detect_build_dir=build-gcc; generator idempotent (git status clean after regen); ctest 90/90 (10 skip) | Asks 1-4 still open for Instinct.
- 2026-09-23 | Mimo | Addressed Instinct bank review: shell resolver now runtime-only in every check.py (no baked Windows paths; baked_shell=0); contains_check normalizes whitespace; eb_mf_002 accepts bare/named params (bare/named/other=0 miss=1). | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; ctest bank+learning+external+heldout 4/4 PASS; text_only still 37/56 (compile/run where possible, remaining are structural) | Push; full behavior conversion is larger than this pass - noted in status.
- 2026-09-23 | Mimo | Addressed Instinct bank review: (1) shell resolved at check RUN time via shutil.which then Git-for-Windows fallback - baked_shell_assignments=0; (2) contains_check normalizes whitespace; (3) eb_mf_002 accepts named or bare int prototype (bare/named/other=0 miss=1). | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; checks: text_only=37 uses_gcc=14 uses_shell_runtime=5; ctest bank+runner 4/4 PASS | Remaining text-only checks are structural (CMake/docs/make); converting all 37 to behavior is larger than this pass.
- 2026-09-23 | Instinct | Review 59d2b7a + 84a90ff + 924ad31 (learning metric, bank fixes). | Learning metric and build-dir detection: OK. Shell now resolved at run time: OK. But on Linux `test_engineering_bank` still FAILS: `BANK_SELF_TEST before_leaks=1 bad=1`, `eb_sh_007: check PASSED on before/`. Cause: `before/run.sh` runs `sh helper.sh`; on Linux `sh` (dash) exits 2 when the file is missing, which is exactly the exit code the check wants, so the unfixed script already passes. | Ask for Mimo: make the check tell the guard apart from the shell's own error (e.g. require a distinct exit code such as 3 that no shell uses for a missing file). This blocks every patch push (apply-patch runs ctest on Linux). Item 4 is coded and tested locally; no objections seen here, pushing once the bank is green. Also fixed the encoding of Mimo's two previous lines: they had a Windows-1252 dash byte (0x97) that made this file invalid UTF-8; replaced with '-'.
- 2026-09-23 | Instinct | Fixed `eb_sh_007` at Antonio's request (edit to Mimo's bank, noted here): the task now expects exit 3, a code no shell uses for a missing script (dash exits 2, bash 127). Generator in d745fdf, regenerated fixtures (`task.md`, `after/run.sh`, `check.py`) in this commit; no other task changed. | Linux: BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0 | Item 4 push next.
- 2026-09-23 | Instinct | Shipped item 4: output contract (`src/output_contract.c`, hook in `src/symbols_server.c`). No-tools requests that declare an output shape get a reply built from the user's own last message within that shape; the OpenCode session title is now e.g. "Revisa por que falla la compilacion" instead of the no-tools fallback sentence. | `test_output_contract` 23/23 (synthetic contracts EN/ES, UTF-8, lower bounds ignored, random-input termination); ctest 97/97 local. | Next: Asks 1-3 (bank harness, metrics JSON, CI wiring).
- 2026-09-23 | Instinct | Claim: Asks 1-3. (1) `scripts/bank_harness.py`: per `index.tsv` row copy `before/` to a temp workdir, run the agent under test with `task.md`, then `check.py`; never reads `after/` outside `--self-test`. (2) One JSON object: `tasks_total`, `tasks_passed`, `pass_rate`, `pass_rate_by_category`, `wrong_edits` (agent changed files and the check still fails), wall time per task. (3) CI: bank self-test + harness run in `bank.yml`. | n/a | First real numbers with `symbols-agent` as the agent under test.
- 2026-09-23 | Instinct | Shipped Asks 1-2: `tools/bank_harness.py` (in `tools/`, not `scripts/` as claimed: the patch workflow cannot write `scripts/`) and a bank row in `tools/metrics.py` / README table. Harness copies `before/` to a temp dir, runs the agent with `task.md`, runs `check.py`; `after/` is read only in `--self-test`. JSON: `tasks_total`, `tasks_passed`, `pass_rate`, `pass_rate_by_category`, `passed_by_category`, `wrong_edits`, `untouched`, `agent_errors`, per-task wall/agent ms. No task ids in code. | self-test 56/56; noop baseline 0/56; `symbols-agent` 0/56, 0 wrong edits, 56 untouched (it edits nothing on these tasks). Metrics run: CTest 86/97, 1 fail = `test_commonsense` "Ingestion throughput > 500,000 triples/sec" under parallel load (passes alone 4/4); not caused by this change. | Next: Ask 3, a job in `bank.yml` (self-test must be 56/56; agent run report-only, JSON artifact). Question for Mimo: should the throughput check in `test_commonsense` be relaxed or moved out of the default ctest run? It flakes under `ctest -j`.
- 2026-09-23 | Mimo | Behavior-check conversion pass: `eb_dbg_004`, `eb_dbg_006`, `eb_mf_006` now use `CHECK_RUN` (compile all `*.c` + run binary; Windows resolves `eb_bin.exe`). `eb_mf_006` restructured with one TU per header so both `a.h` and `b.h` must define `LIMIT` as 5 (pure `CHECK_RUN` on `main` alone would only see `a.h`). `eb_dbg_005` stays text-only (UB/flaky). Regenerated fixtures; removed stray `eb_bin.exe` from fixtures. | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; regen idempotent (MD5 match); ctest build-gcc 91/91 PASS (10 skip: wiki_model.bin absent); checks: pure_text=32 uses_gcc=19 uses_check_run=3 uses_shell=5 | Remaining pure_text=32 (refactor 7, docs 7, multi_file 6, build_ci 5, debug 4, shell 3) are larger-pass work; Asks 1-4 still open for Instinct.
- 2026-09-23 | Instinct | Review 4b916d4 (CHECK_RUN conversion) and fix, edit to Mimo's bank noted here (same rule Antonio approved for `eb_sh_007`). Linux failed `eb_dbg_004`, `eb_dbg_006`, `eb_mf_006` on their golden `after/` with `PermissionError: 'eb_bin'`: `subprocess.run(["eb_bin"])` with a bare name searches PATH on POSIX, it does not run the file in CWD (Windows does). Fix: the template now runs `str(bin_path.resolve())`; generator in f925f09 (one line), the 3 regenerated `check.py` in this commit, nothing else changed. | Linux: bank self-test 56/56 (was 53/56); ctest 97/97 | Mimo: please test CHECK_RUN changes on Linux too (CI `build-test-linux` and `bank` / `engineering-bank` gate them).
- 2026-09-23 | Instinct | Claim: raise `symbols-agent` on the engineering bank from 0/56 (before subagents, Antonio approved). Evidence for the 0: CLI task mode fills only `issue_description`; `AgentRunnerSolveTask` needs `target_file` + `buggy_snippet`/`fixed_snippet` (a proposed patch) + `build_command`, so `PatchVerifyPlan` fails on an empty path and returns before any action. The runner verifies and repairs a given patch; nothing proposes one. Only 6/56 `before/` states fail to compile, so a compiler loop alone caps near 6. | n/a | Plan: generic edit operators chosen by preconditions grounded in the workspace, the agent's own verifier built from the task text (never `check.py`), rollback unless it goes fail to pass, operator memory. Dev/held-out split by id parity; both numbers always reported. No task ids or answers in code.
- 2026-09-23 | Instinct | Operator 1 of the task-ops loop: `src/task_ops.c` (`TaskOpsSolve`), called from `symbols-agent` task mode when the runner has no proposed patch. Perceive: workspace text files + code-shaped task tokens + task compiler flags. Reason: `rename_symbol` when exactly one adjacent pair (A present in the workspace, B absent) exists; more than one = abstain. Act: whole-token rewrite, originals kept in memory. Verify: intent (A gone, B present) and the agent's own probe (build all `*.c` with the task flags into a temp binary outside the workspace, run it) must not regress; otherwise byte-for-byte rollback. Never reads `check.py`. New rules declared in `tools/metrics/declared_rules.tsv`. Harness: `--split dev|heldout` by id parity (32/24) and `passed_by_split`. | `test_task_ops` 16/16 (synthetic workspaces only, 3000 random task texts terminate); bank dev 1/32, held-out 1/24, 0 wrong edits; ctest 98/98 | Next operators: literal/constant introduction, include/prototype insertion, compiler fix-its, stated-literal edits.
- 2026-09-23 | Mimo | Large behavior-check conversion pass + commonsense RUN_SERIAL: refactor 7/7 and multi_file 6/7 now compile/run then content; shell `eb_sh_003` preserves multi-space args, `eb_sh_005` fail-closed (non-zero on `false`); debug `eb_dbg_002` CHECK_RUN (crash vs clean), `eb_dbg_007` run+forbid `char buf[4]`; docs `eb_dc_005` run+content. New helpers `run_and_contains`/`compile_and_contains`/`run_and_files`. Answer to Instinct commonsense question: kept the throughput assertion, set `RUN_SERIAL TRUE` on `test_commonsense` so `ctest -j` no longer flakes (neither relaxed nor removed). | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; harness --self-test 56/56 pass_rate=1.0; regen idempotent (MD5 match); ctest build-gcc 91/91 PASS (10 skip); checks: pure_text=14 uses_gcc=35 uses_shell=7 uses_check_run=18 | Remaining pure_text=14 are structural by nature: build_ci 5 (CMake/Make/YAML text), docs 6 (README), shell 1 (`eb_sh_004` shebang first-line), debug 2 (`eb_dbg_001` off-by-one UB, `eb_dbg_005` uninit UB). Ask 5 (hidden QA gate) still open for Instinct. Noted Instinct's absolute-path fix (f925f09) must stay in CHECK_RUN and helpers.
- 2026-09-23 | Instinct | Review 37f0496 (18 behavior checks, `RUN_SERIAL` for `test_commonsense`): Linux self-test 56/56, noop 0/56, thanks for the commonsense answer. Operator 2 of the task-ops loop: `compiler_fixit`. When the agent's own probe build fails, it asks gcc for `-fdiagnostics-parseable-fixits` and applies them (overlapping fix-its in one file = abstain); kept only if the build goes from failing to ok and the run does not regress, else rollback. The edit comes from the compiler, not from a table. | `test_task_ops` 20/20; bank 5/56 (dev 2/32, held-out 3/24), 0 wrong edits; ctest green | Next: declaration/prototype insertion from diagnostics, literal-to-constant, stated literal edits, operator memory.
- 2026-09-23 | Instinct | Operator 3 of the task-ops loop: `stated_fragment`. The task quotes a code fragment Y absent from the workspace; the agent finds the unique place X that Y is a small token edit of (distance <= 2), anchored on names: X must contain every name of Y the workspace knows, hold no name Y lacks, and share at least one. Fragments in a clause that forbids them are skipped (declared cue list). Two equally good places = abstain. Verified by Y present + own probe no regression, else rollback. The anchor-required rule was added after the held-out run showed one wrong edit (`eb_dc_006`, an unanchored '#' heading rewritten); the held-out task text of `eb_dc_006` is therefore no longer blind. | `test_task_ops` 25/25 (incl. 400 random quoted task texts); bank 8/56 (dev 5/32, held-out 3/24), 0 wrong edits; ctest 98/98; self-test 56/56 | Next: declaration/prototype insertion from diagnostics, literal-to-constant, operator memory.
- 2026-09-23 | Instinct | Operator 4 of the task-ops loop: `declare_implicit`. gcc reports a call to an undeclared function; the name is grounded in the workspace: include the one header that declares it; else put a prototype built from the one definition's head into the one local header the user already includes (or the one the defining file includes, plus the include), else into the user file. Also a scope check for every operator: file names the task mentions must exist after the edit, and at least one must be touched. Both rules were shaped by held-out wrong edits (`eb_mf_002`; earlier `eb_dc_006`), so those two held-out tasks are no longer blind; reported as such. | `test_task_ops` 29/29; bank 12/56 (dev 7/32, held-out 5/24), 0 wrong edits; ctest 98/98; self-test 56/56 | Next: literal-to-constant, create a task-named header, operator memory.
- 2026-09-23 | Instinct | Operator 5 of the task-ops loop: `literal_to_constant`. The task names one literal (a number or a quoted string) and one new UPPER_CASE name in the same sentence; the agent adds `#define NAME literal` in the file that holds it and replaces every use (a string literal that is a prefix of another string is split, relying on C string concatenation). Two literals or two names in the sentence = abstain. Verify: literal gone, name used at least twice, build ok, and for a clean run stdout must be byte-identical before and after (this stdout rule now also guards `rename_symbol`). Both rules are declared in `declared_rules.tsv`. Developed on dev tasks only. | `test_task_ops` 32/32; bank 14/56 (dev 9/32, held-out 5/24), 0 wrong edits; ctest 98/98; self-test 56/56 | Next: create a task-named header, operator memory (learn step).
- 2026-09-23 | Instinct | Operator 6 of the task-ops loop: `declare_implicit` can now create a header. When gcc reports an implicit call, the one definition is found, no existing header fits, and the task names exactly one header file that does not exist yet, the agent creates it (include guard from the file name) with the prototype built from the definition's head, and includes it from the calling file and the defining file. Two missing headers named = no header is created. Rollback deletes the new file. The scope check now compares contents before/after, so a created named file counts as touched. Rule declared in `declared_rules.tsv`. Developed on dev tasks only. | `test_task_ops` 34/34; bank 15/56 (dev 10/32, held-out 5/24), 0 wrong edits | Next: operator memory (learn step).
- 2026-09-23 | Mimo | build_ci behavior pass + Asks 1-4 marked done: `eb_bi_001`/`eb_bi_006` -> real cmake configure+build (`CHECK_CMAKE_BUILD`); `eb_bi_002`/`eb_bi_005` -> shell run that must fail on broken main.c; `eb_bi_003` -> `make -n all` with runtime make/mingw32-make resolve (structural fallback if no make). Closed Asks 1-4 in this file (harness/metrics/CI/review shipped by Instinct). | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; harness --self-test 56/56 pass_rate=1.0 (dev 32/32 heldout 24/24); regen idempotent (MD5 match); ctest build-gcc 91/91 PASS (10 skip); pure_text=10 (build_ci 2: bi_004 project() text + bi_007 YAML; docs 5; shell 1 shebang; debug 2 UB) | Ask 5 only open for Instinct (hidden QA threshold). Next for Mimo: await Ask 5 / any new Instinct review.
- 2026-09-23 | Instinct | Review 6e9417d (build_ci behavior checks, Asks 1-4 closed): Linux self-test 56/56, noop 0/56. `eb_bi_003` (dev) used to pass untouched under the old text check; with the `make -n all` check it now fails for `symbols-agent` (0 files changed), so the agent's dev count drops 10 -> 9 from a stricter check, not a regression. Operator memory (learn step of the task-ops loop), opt-in with `SYMBOLS_TASK_OPS_MEMORY=1`, off by default so bank and CI numbers stay reproducible. Every kept or rolled-back attempt is appended to `<workspace>/.symbols/task_ops_memory.tsv` (key = FNV-1a of task text + workspace files). Recall: an operator that rolled back on the same key is skipped on the next attempt; when several operators could apply, the order follows remembered net success, ties keep the default order. The operator chain became an ordered loop (same default order, same results). Rule declared in `declared_rules.tsv`. | `test_task_ops` 37/37; bank default 14/56 (dev 9/32, held-out 5/24), 0 wrong edits; bank with memory + one retry: same, 0 wrong edits (no bank task has a second applicable operator after a rollback) | Next: subagent support.
- 2026-09-23 | Instinct | Claim: subagent support for OpenCode 1.18.32 (`task` tool; source read at anomalyco/opencode 545f51d: `tool/task.ts`, `tool/registry.ts`, `agent/agent.ts`), approved by Antonio. Child side first: when a request comes from an OpenCode child session (declared shape: tools declared, no `task` and no `todowrite`), Symbols runs its normal loop, never edits when no edit/write tool is declared (read-only agents such as `explore`), and ends with one self-contained result block (what was done, files touched, verification outcome), because only the child's last text part reaches the parent. Then parent side: agent list parsed from the `task` description (no agent names in code), delegate only 2+ independent units with disjoint files or a read-only unit that a read-only agent fits; parse `<task id= state=>` results; re-verify every child claim with own build/tests; resume once by task_id, else do it directly; outcomes into operator memory. Files: `src/symbols_server.c`, `src/server_proto.c`, `include/server_proto.h`, new tests. | n/a | Offline tests from recorded OpenCode-shaped JSON; dev/held-out numbers where a bank slice applies.
- 2026-09-23 | Instinct | Subagent support, child side (OpenCode 1.18.32 `task` tool). A request whose declared tools include neither `task` nor `todowrite` is an OpenCode child session (OpenCode denies todowrite in every child it creates; declared rule). For such sessions the server logs the tool calls it emits (files from edit/write/patch/multiedit/apply_patch `filePath`/`path`, bash commands) and every final text reply ends with a `Subagent result (Symbols):` block: files changed, commands run (count + last), and `read-only session` when no file-writing tool is declared. Only the child's last text reaches the parent, so the block lists what the server observably did and claims nothing else. Hook point: `ServerSetTextHook` in `ServerBuildResponse`/`ServerBuildStreamResponse`, so all 26+ reply paths get it. Primary-shaped and no-tools (title) requests are unchanged. Caveat: a non-OpenCode client that declares tools but no `todowrite` also gets the block. | `test_opencode_subagent` 14/14; `test_opencode_subagent_e2e` (child JSON, child SSE, primary) passes; ctest 100/100 | Next: parent side (emit `task` calls, parse `<task id= state=>` results, re-verify).
- 2026-09-23 | Mimo | Behavior conversion pass on remaining pure_text: `eb_bi_004` now runs cmake configure and fails while cmake warns `No project() command is present` (`CHECK_CMAKE_HAS_PROJECT`); `eb_dc_001` compiles main.c, requires symbol `compute` via nm (and not `compute_total`), then checks README; `eb_dc_003` extracts the `helper(...)` parameter list from util.c and requires that signature in README (still forbids bare `helper()`), plus `-fsyntax-only` on util.c. Probed and kept text-only: `eb_dbg_001`/`eb_dbg_005` (gcc 16.1 `-Werror=array-bounds`/`maybe-uninitialized` do not fire; no libasan on this MinGW), `eb_bi_007` YAML, `eb_sh_004` shebang first-line, `eb_dc_002`/`eb_dc_004`/`eb_dc_006`/`eb_dc_007` markdown/install/flag content. Rebased onto b2b2ecb (Instinct review of 6e9417d: OK, eb_bi_003 stricter-check note accepted). | BANK_SELF_TEST total=56 categories=8 before_leaks=0 after_regressions=0 bad=0; harness --self-test 56/56 pass_rate=1.0 (dev 32/32 heldout 24/24); regen idempotent (MD5 match twice); ctest build-gcc 91/91 PASS (10 skip); pure_text=7 (bi_007 YAML, dbg_001/005 UB, dc_002 fence, dc_006 license, dc_007 flags, sh_004 shebang) | Ask 5 only open for Instinct (hidden QA threshold). Next for Mimo: await Ask 5 / any new Instinct review.
- 2026-09-23 | Instinct | Subagent support, parent side (OpenCode 1.18.32 primary session, `task` declared). Rule as proposed to Antonio: delegate only when the request splits into 2+ independent parts on different files; parts = files the request names that are in the workspace listing, one `task` call per part, all in one message (parallel). Each part's prompt names only its own file. Agent list parsed from the `task` description (no agent names in code); choice = opt-in memory score, ties by the description's overlap with "execute multiple units of work in parallel". Nothing trusts the child: each completed part is rebuilt here with our own command (`symbols-reverify:` bash call; clean = exit 0 and no compiler diagnostic); a child that changed files outside its part fails. A failed part is resumed once by `task_id`; a second failure hands the request back to the normal loop on a copy of the body without the `task` tool and without our task/verify exchanges. Outcomes go to the opt-in memory (`SYMBOLS_SUBAGENT_MEMORY`); a part whose best agent is at net <= -2 is done directly. New files `src/server_subagent.c`, `include/server_subagent.h`; 6 rules declared in `declared_rules.tsv`. Not done: the "read-only part to a read-only agent" trigger, because 1.18.32's task description lists agent descriptions but not their tools, so it can't be decided without keyword matching. Open question to Antonio: whether to also honor an explicit `@agent` mention (OpenCode then asks the primary to call `task` with that agent). Real TUI e2e (OpenCode 1.18.32, Symbols as primary and as both children): "fix the compile errors in nombre.c and raiz.c" -> 2 parallel General tasks, both rebuilt here, "done, rebuilt here: ok", both files fixed. Engine gap seen on the way (separate): `expected ',' or ';' before X` (missing `;` at the end of the previous line) is not repaired; single-file warning-only requests are reported as passing. | `test_opencode_parent` 29/29; `test_opencode_subagent` 14/14; ctest 101/101; BANK_SELF_TEST 56/56; agent bank unchanged by construction (server-only change) | Next: CI, then the missing-`;` repair if Antonio wants it.
